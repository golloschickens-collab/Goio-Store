# ==================================================
# == Variables de Entorno para el Stack del CRM    ==
# ==================================================
#
# Copia este archivo a .env y reemplaza los valores
# con tus propias claves seguras para producción.
#

# --- Base de Datos (PostgreSQL) ---
# Contraseña para el usuario 'crm_user' de la base de datos.
# IMPORTANTE: Cambiar esta contraseña en producción.
CRM_DB_PASSWORD=crm_secure_pass_2025

# --- API (FastAPI) ---
# Clave secreta para firmar tokens JWT de la API.
# Debe ser una cadena larga y aleatoria.
CRM_API_SECRET=change_this_secret_key_in_production

# Entorno de ejecución de la API (development, staging, production)
ENVIRONMENT=development

# --- NocoDB ---
# Clave secreta para firmar los tokens JWT de NocoDB.
# IMPORTANTE: Cambiar esta clave en producción.
NOCODB_JWT_SECRET=nocodb_jwt_secret_change_this
